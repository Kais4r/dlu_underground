/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "plus.unsplash.com",
      "imagor.owtg.one", // Add this domain
    ],
  },
};

export default nextConfig;
